# Prompt Log

This file tracks prompts used in this project as well as a picture of my local WindSurf setup.

![Local WindSurf setup](image.png)

```markdown
Prompt: As a Senior Machine Learning Engineer, create a project to predict the service for a patients using the following dataset https://huggingface.co/datasets/electricsheepafrica/Nigeria-hospital-patients. Show the initial EDA analysis and a more specific EDA analysis with service as the target all showing visualizations. As part of the project, create the file preprocessing_utls.py which contains reusable functions and code to generate derived features, load data from Hugging Face, compute basic statistics, generate EDA visualizations, and build a robust scikit-learn preprocessing pipeline.
```

```markdown
  Prompt: Can you separate the plots generated during initial EDA into a separate folder under figures/eda and update the display loop
```  

```markdown
  Prompt: Create another set of EDA visualizations with specifically service as the target
```

```markdown
  Prompt: Create the files potential_issues that will be a generated file capturing a summary of the data, potential issues, sentinel scans and final recommendations
```

```markdown
  Prompt: Create derived features from the arrival date and age fields
```  

```markdown
  Prompt: Add a derived feature showing the Nigerian seasons based on the month in the arrival_date column
```

```markdown
  Prompt: Add derived features arrival_dow, arrival_part_of_month and arrival_quarter.
```

```markdown
  Prompt: Add to potential_issues.md why derived features were not created on departure_date
```

```markdown
  Prompt: Design a reusable preprocessing pipeline with numeric, categorical, and datetime branches
```  

```markdown
  Prompt: Summarize these model performance numbers and what they mean
```  

```markdown
  Prompt: Could you do another prediction but using Random Forest Classifier?
```

```markdown
  Prompt: Compare performances from the baseline model and the random forest model
```

```markdown
  Prompt: Create a file called insights.md that will capture a summary of the data, potential issues, sentinel scans and final recommendations
```

```markdown
  Prompt:Targeted debugging prompts (e.g., "Explain this `ValueError` in the preprocessing pipeline and how to fix it")
```

```markdown
  Prompt: "Design a reusable preprocessing pipeline with numeric, categorical, and datetime branches"
```

```markdown
  Prompt: "Summarize these model performance numbers and what they mean"
```