# Potential Issues and Distribution Signals (Generated)

Generated by: notebooks/patients_eda_preprocessing.ipynb
Dataset: hf://datasets/electricsheepafrica/Nigeria-hospital-patients/patients.parquet

## Summary (from reports)
- Rows: 1000, Columns: 7, Memory: ~0.3 MB
- Dtypes (schema_dtypes.csv):
  - patient_id: object
  - name: object
  - age: int64
  - arrival_date: datetime64[ns]
  - departure_date: datetime64[ns]
  - service: object
  - satisfaction: int64
- Unique values (overview.json):
  - patient_id: 1000, name: 1000, age: 90
  - arrival_date: 344, departure_date: 337, service: 4, satisfaction: 40
- Missingness (missing_values.csv): all columns 0% missing
- Numeric stats (numeric_describe.csv):
  - age: mean 45.34, std 26.00, min 0, max 89
  - satisfaction: mean 79.60, std 11.55, min 60, max 99

## Potential Issues
- Identifiers in features
  - patient_id and name have 1000 unique values each (likely identifiers) and therefore high cardinality
  - Risk: leakage, overfitting, unnecessary dimensionality if one-hot encoded.

- No missing values reported
  - 0% missing for all columns is atypical for hospital data; may indicate sentinel values (e.g., "Unknown", empty strings, 0).

- Age distribution considerations
  - Range 0–89 with mean ~45; zeros may represent newborns or placeholders.
  - Verify that 0 is valid; consider binning or spline transforms for modeling.

- Satisfaction distribution considerations
  - Bounded range 60–99 with 40 unique values; may be ordinal/score with ceiling effects.
  - If used as target, evaluate regression vs. ordinal classification.

- High-cardinality categoricals
  - name and patient_id are high-cardinality; one-hot leads to many columns. A feature is deemed as high-cardinality if it has a distinct-to-row ratio of 0.8 or higher. This could increase model complexity and overfitting risk.
  - service has only 4 categories; fine, but check class balance.

- Temporal coverage and potential leakage
  - 344 unique arrival_date days (~year-scale coverage). Temporal patterns likely (dow/seasonality).
  - Use time-aware splits to avoid leakage if modeling time-sensitive outcomes.
  - For the current task (predicting `service` at admission), no derived features are created from `departure_date` (e.g., length_of_stay, departure month/weekend) because `departure_date` is only observed after the service has been chosen and care delivered; using it would introduce strong target leakage.

- Missing derived temporal features
  - length_of_stay = departure_date - arrival_date not yet derived; often predictive.

## Sentinel scan results (generated)
- Scan configuration:
  - Text tokens (case-insensitive): Unknown, UNK, N/A, NA, None, empty string
  - Numeric check: exact zeros (0) in numeric columns
- Findings from `reports/sentinel_scan.csv`:
  - age: 12 zeros detected
  - satisfaction: 0 zeros detected
  - patient_id, name, service: 0 text sentinel matches
  - arrival_date, departure_date: datetime columns; text sentinels not applicable
- Notes:
  - Zeros can be legitimate (e.g., newborn age = 0). Treat as a signal to review rather than an error.
  - No common text sentinels were found in object columns.
  - Age zeros appear to represent real infants; no imputation applied; consider `is_infant` feature although unnecessary as age will have a derived feature, age bucket, associated with it
  - External validation: Nigerian under-five and neonatal burden is substantial; see PMC article supporting plausibility of infant cases: https://pmc.ncbi.nlm.nih.gov/articles/PMC10894548/#:~:text=A%20total%20of%2011%2C416%20under,cause%20of%20under%2Dfive%20mortality.

## Recommendations
- Exclude identifiers from features
  - Drop patient_id and name from feature set prior to preprocessing/modeling.

- Check for sentinel missingness
  - Scan for placeholders like "Unknown", empty strings, zeros in inappropriate contexts.

- Add derived features and temporal flags
  - length_of_stay_days, dayofweek/weekend/month indicators.
  - Current engineered features in the preprocessing pipeline include:
    - `age_bucket`: groups continuous age into clinically interpretable bands (child, young_adult, adult, middle_aged, elderly), allowing the model to capture non-linear age effects on service choice.
    - `arrival_date_month`: extracts the calendar month of arrival as an integer, so seasonal patterns in service usage (e.g., more emergency cases during certain months) can be learned.
    - `arrival_date_weekday` / `arrival_date_weekend`: boolean flags indicating whether the arrival occurred on a weekday (Mon–Fri) or weekend (Sat–Sun), capturing differences in service utilization by day-of-week (e.g., more emergency visits on weekends).
    - `arrival_dow`: categorical day-of-week derived from `arrival_date` (0=Mon..6=Sun), allowing the model to learn finer-grained weekday patterns beyond a simple weekday/weekend split.
    - `arrival_part_of_month`: categorical indicator of whether the arrival occurred in the early (days 1–10), mid (11–20), or late (21+) part of the month, to capture any scheduling or billing-cycle effects.
    - `arrival_quarter`: calendar quarter (1–4) derived from `arrival_date`, providing a coarser seasonal signal than month.
    - `arrival_season`: maps the month of `arrival_date` into a simplified Nigerian season (`dry` for Nov–Mar, `rainy` for Apr–Oct), so the model can learn coarse seasonal patterns in service demand.

- Handle high-cardinality categoricals safely
  - Set OHE min_frequency (e.g., 10) or drop quasi-identifiers; avoid dense OHE on IDs.
  - One-Hot Encoding is used for categorical variables so that the preprocessing pipeline produces a fully numeric feature matrix compatible with most scikit-learn models, while preserving category information in a model-agnostic way (no artificial ordering of categories).

- Numeric and datetime scaling
  - Numeric features (e.g., `age`, `arrival_date_month`) and datetime-derived features (e.g., `arrival_date__year`, `arrival_date__dow`) are standardized with `StandardScaler` (z-score: subtract training mean, divide by training std).
  - This keeps feature scales comparable, improves optimization for linear models like multinomial logistic regression, and prevents large-scale features from dominating the loss purely due to units.

- Use time-aware validation
  - Split train/test by time to respect chronology and reduce leakage risk.

- Avoid target leakage
  - If satisfaction is a target, ensure it is excluded from features during preprocessing.
  - When the modeling target is `service`, treat `satisfaction` and `satisfaction_bucket` as **outcome variables**, not inputs: they are only observed *after* service has been chosen and care delivered. Using them as features would leak future information into the model and inflate performance compared to a real admission-time decision setting.
  - Similarly, `departure_date` (and any features derived from it, such as length_of_stay or departure month/weekend) is only known after admission and completion of care. For a `service` prediction model intended to operate at arrival time, these should be excluded from the feature set because they encode post-decision information and therefore constitute strong target leakage.

## Data sources
- reports/overview.json
- reports/schema_dtypes.csv
- reports/missing_values.csv
- reports/numeric_describe.csv
- reports/categorical_cardinality.csv
